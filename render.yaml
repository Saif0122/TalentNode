services:
  - type: web
    name: talentnode-backend
    env: node
    buildCommand: npm install
    startCommand: npm run start:backend
    envVars:
      - key: PORT
        value: 5001
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
